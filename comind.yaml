competition:
  id: null         
  input_dir: null  
  task_desc: null  

llm:
  model: gpt-5-mini
  max_retries: 20
  timeout: 600

agent:
  num_code_agents: 2 
  num_iterations: 20
  num_iterations_code_agents: 30
  time_limit_code_agents: 18000

  max_referred_discussions: 10
  max_referred_kernels: 1

  workspace_dir: workspace
  external_data_dir: datasets 
  submission_file_name: submission.csv
  separate_validation_submission: True
  reproduce: False

execution:
  inspect_interval: 1200
  timeout: 18000
  max_cpu_cores: 24
  max_gpu_count: 1